# Custom Agent Rules

- **Do NOT run the `browser_subagent` or open a browser page automatically.** Always ask the user for explicit permission in text before launching any browser subagents or opening browser URLs.
